# Pinned rule set for `ruff check`: syntax errors and pyflakes essentials.
# Deliberately narrow - this codebase chooses its own style for the rest
# (e.g. long protocol strings, exec/eval as the entire point of a REPL).
[lint]
select = ["E4", "E7", "E9", "F"]
