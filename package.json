{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "opencode-pyrepl",
  "version": "0.1.0",
  "type": "module",
  "description": "opencode plugin giving the agent a stateful Python REPL",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/noonomyen/opencode-pyrepl-plugin"
  },
  "main": "./src/pyrepl.ts",
  "exports": {
    ".": "./src/pyrepl.ts",
    "./server": "./src/pyrepl.ts",
    "./package.json": "./package.json"
  },
  "files": [
    "src/",
    "README.md"
  ],
  "scripts": {
    "deploy": "bun src/install.ts",
    "build": "bun src/build.ts",
    "build:js": "bun src/build.ts --js-only",
    "build:py": "bun src/build.ts --py-only",
    "typecheck": "tsc --noEmit",
    "lint": "ruff check src/",
    "test": "bun test tests/",
    "test:fast": "bun test tests/protocol.test.ts tests/tools.test.ts tests/config.test.ts",
    "test:slow": "bun test tests/slow.test.ts",
    "ci": "bun run typecheck && bun run lint && bun run build && bun test tests/",
    "prepack": "find src tests -name __pycache__ -type d -prune -exec rm -rf {} +"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.18.29"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.18.29",
    "@types/node": "26.2.0",
    "bun-types": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
